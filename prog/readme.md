Create a .env file put this in:
`CONNECTION_STRING=Host=localhost;Database=<dbname>;Username=postgres;Password=<password>`

run the application with the path to the .env file in quotation marks as the argument: `program.exe "<path_to_.env_file>"`

